# Python
backend/.venv/
__pycache__/
*.pyc
*.egg-info/

# Node (vendored output under app/static/{vendor,css,fonts} IS committed --
# only the package manager cache/tree is ignored)
backend/node_modules/

# Env / secrets
.env
backend/secrets/
*.pem
*.p12

# Database backups (backup.sh output) -- real lead/business data, never
# belongs in git history. Copy these off-box yourself (see README's
# Backups section), don't commit them.
backups/
*.sql.gz
backup.log

# Editor / OS
.DS_Store
*.swp

# Alembic
backend/alembic/versions/__pycache__/
